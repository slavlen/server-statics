server {
    listen 80;

    location /images {
        root /var/www;
        autoindex on;
        expires {{ nginx.cache_time }};
        gzip on;
    }

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}