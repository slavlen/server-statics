version: '3.8'

services:
  ansible_test:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2222:22"
      - "8080:80"
    volumes:
      - ./ansible:/ansible
    container_name: ansible_test